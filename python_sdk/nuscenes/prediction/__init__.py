# Comment this out because it causes issues with using the nuscenes devkit
# code as a bazel dependency.
# This will likely impair its usage outside of bazel.
# from .helper import PredictHelper, convert_global_coords_to_local, convert_local_coords_to_global
